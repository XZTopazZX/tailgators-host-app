Tail-Gators Host Stand PWA

Open index.html in a browser to test locally. To install on iPad, host the folder online, open the URL in Safari, tap Share, then Add to Home Screen.

This first version stores data in the tablet browser using localStorage. That means it works free, but multiple tablets will not sync yet.
